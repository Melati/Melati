
package org.melati.poem.test;


abstract table EmptyAbstractTable

  (displayname = "Empty Abstract")
  (description = "Empty abstract table to create a stub")
  (displayorder = 50)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
}

table EverythingNormal

  (displayname = "EverythingNormal")
  (description = "Every datatype in one table")
  (displayorder = 60)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;
    byte[] binaryfield
      (displayname = "Binaryfield")
      // (description = null)
      (uneditable)
      (nullable)
      (size = 13)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 1)
      (uncreateable)
      (displaylevel = summary)
      (searchability = no)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    String stringfield
      (displayname = "String Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 2)
      // usercreateable
      (displaylevel = primary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Password passwordfield
      (displayname = "Password Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 3)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Boolean booleanfield
      (displayname = "Boolean Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 4)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Date datefield
      (displayname = "Date Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 5)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Double doublefield
      (displayname = "Double Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 6)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Integer integerfield
      (displayname = "Integer Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 7)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Long longfield
      (displayname = "Long Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 8)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    BigDecimal bigdecimalfield
      (displayname = "BigDecimal Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      (precision = 22)
      (scale = 2)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 9)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Timestamp timestampfield
      (displayname = "Timestamp Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 10)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Boolean deletedfield
      (displayname = "Deleted Field")
      // (description = null)
      // usereditable
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 11)
      //usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      (deleted)
      ;
}

table ENExtended extends EverythingNormal

  (displayname = "Everything Normal Extended")
  (description = "Every datatype inheritted from a normal table and normally defined")
  (displayorder = 70)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    byte[] binaryfield2
      (displayname = "Binaryfield2")
      (description = "Second Binary Field")
      (uneditable)
      (nullable)
      (size = 13)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 11)
      (uncreateable)
      (displaylevel = summary)
      (searchability = no)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    String stringfield2
      (displayname = "String Field 2")
      (description = "Second String Field")
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 20)
      // usercreateable
      (displaylevel = primary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      (unique)
      ;
    Password passwordfield2
      (displayname = "Password Field 2")
      (description = "Second Password Field")
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 13)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Boolean booleanfield2
      (displayname = "Boolean Field 2")
      (description = "Second Boolean Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 14)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Date datefield2
      (displayname = "Date Field 2")
      (description = "Second Date Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 15)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Double doublefield2
      (displayname = "Double Field 2")
      (description = "Second Double Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 16)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Integer integerfield2
      (displayname = "Integer Field 2")
      (description = "Second Integer Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 17)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Long longfield2
      (displayname = "Long Field 2")
      (description = "Second Long Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 18)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    BigDecimal bigdecimalfield2
      (displayname = "BigDecimal Field 2")
      (description = "Second BigDecimal Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      (precision = 22)
      (scale = 2)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 19)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Timestamp timestampfield2
      (displayname = "Timestamp Field 2")
      (description = "Second Timestamp Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 20)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}



abstract table EverythingAbstract

  (displayname = "EverythingAbstract")
  (description = "Every datatype in one abstract table")
  (displayorder = 55)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    byte[] binaryfield
      (displayname = "Binaryfield")
      // (description = null)
      (uneditable)
      (nullable)
      (size = 13)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 1)
      (uncreateable)
      (displaylevel = summary)
      (searchability = no)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    String stringfield
      (displayname = "String Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 2)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Password passwordfield
      (displayname = "Password Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 3)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Boolean booleanfield
      (displayname = "Boolean Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 4)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Date datefield
      (displayname = "Date Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 5)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Double doublefield
      (displayname = "Double Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 6)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Integer integerfield
      (displayname = "Integer Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 7)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Long longfield
      (displayname = "Long Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 8)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    BigDecimal bigdecimalfield
      (displayname = "BigDecimal Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      (precision = 22)
      (scale = 2)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 9)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Timestamp timestampfield
      (displayname = "Timestamp Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 10)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}



table EAExtended extends EverythingAbstract

  (displayname = "Everything Abstract Extended")
  (description = "Every datatype inheritted from an abstract table and normally defined")
  (displayorder = 80)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 10)
      (uncreateable)
      (displaylevel = record)
      (searchability = yes)
      (displayorderpriority = 0)
      // Not sortdescending
      (indexed)
      (unique)
      ;
    byte[] binaryfield2
      (displayname = "Binaryfield2")
      (description = "Second Binary Field")
      (uneditable)
      (nullable)
      (size = 13)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 11)
      (uncreateable)
      (displaylevel = summary)
      (searchability = no)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    String stringfield2
      (displayname = "String Field 2")
      (description = "Second String Field")
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 20)
      // usercreateable
      (displaylevel = primary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      (unique)
      ;
    Password passwordfield2
      (displayname = "Password Field 2")
      (description = "Second Password Field")
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 13)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Boolean booleanfield2
      (displayname = "Boolean Field 2")
      (description = "Second Boolean Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 14)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Date datefield2
      (displayname = "Date Field 2")
      (description = "Second Date Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 15)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Double doublefield2
      (displayname = "Double Field 2")
      (description = "Second Double Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 16)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Integer integerfield2
      (displayname = "Integer Field 2")
      (description = "Second Integer Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 17)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Long longfield2
      (displayname = "Long Field 2")
      (description = "Second Long Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 18)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    BigDecimal bigdecimalfield2
      (displayname = "BigDecimal Field 2")
      (description = "Second BigDecimal Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      (precision = 22)
      (scale = 2)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 19)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    Timestamp timestampfield2
      (displayname = "Timestamp Field 2")
      (description = "Second Timestamp Field")
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 20)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}



////////////////////////////////////


table BinaryField
  (displayname = "Binary Field")
  (description = "A table with only a binary field in it")
  (displayorder = 10)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;
    byte[] binaryfield
      (displayname = "Binaryfield")
      // (description = null)
      (uneditable)
      (nullable)
      (size = 13)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 1)
      (uncreateable)
      (displaylevel = summary)
      (searchability = no)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    byte[] unlimited
      (displayname = "Binaryfield")
      // (description = null)
      (uneditable)
      (nullable)
      (size = unlimited)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 1)
      (uncreateable)
      (displaylevel = summary)
      (searchability = no)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}



table StringField

  (displayname = "String Field")
  (description = "A table with only a string field in it")
  (displayorder = 12)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    String stringfield
      (displayname = "String Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 2)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
    String unlimited
      (displayname = "Unlimited String Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = unlimited)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 3)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table PasswordField

  (displayname = "Password Field")
  (description = "A table with only a password field in it")
  (displayorder = 15)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Password passwordfield
      (displayname = "Password Field")
      // (description = null)
      // usereditable
      (nullable)
      (size = 23)
      (width = 20)
      (height = 3)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 3)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table BooleanField

  (displayname = "Boolean Field")
  (description = "A table with only a boolean field in it")
  (displayorder = 18)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Boolean booleanfield
      (displayname = "Boolean Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 4)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table DateField

  (displayname = "Date Field")
  (description = "A table with only a date field in it")
  (displayorder = 20)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Date datefield
      (displayname = "Date Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 5)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table DoubleField

  (displayname = "Double Field")
  (description = "A table with only a double field in it")
  (displayorder = 22)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Double doublefield
      (displayname = "Double Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 6)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table IntegerField

  (displayname = "Integer Field")
  (description = "A table with only an integer field in it")
  (displayorder = 24)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Integer integerfield
      (displayname = "Integer Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 7)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table LongField

  (displayname = "Long Field")
  (description = "A table with only a long field in it")
  (displayorder = 28)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Long longfield
      (displayname = "Long Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 8)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}

table BigdecimalField

  (displayname = "Big Decimal  Field")
  (description = "A table with only a big decimal field in it")
  (displayorder = 30)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    BigDecimal bigdecimalfield
      (displayname = "BigDecimal Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      (precision = 22)
      (scale = 2)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 9)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}
table TimestampField

  (displayname = "Timestamp  Field")
  (description = "A table with only a timestamp field in it")
  (displayorder = 32)
  // (defaultcanread = null)
  // (defaultcanwrite = null)
  // (defaultcandelete = null)
  // (cancreate = null)
  (cachelimit = 100)
  // Not seqcached
  (category = "Normal")
{
    Integer id
      (primary)
      (displayname = "Id")
      // (description = null)
      (uneditable)
      // Not nullable
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      (displayorder = 0)
      (uncreateable)
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      (indexed)
      (unique)
      ;

    Timestamp timestampfield
      (displayname = "Timestamp Field")
      // (description = null)
      // usereditable
      (nullable)
      (width = 20)
      (height = 1)
      // (renderinfo = null)
      // (rangelow_string = null)
      // (rangelimit_string = null)
      (displayorder = 10)
      // usercreateable
      (displaylevel = summary)
      (searchability = yes)
      // (displayorderpriority = null)
      // Not sortdescending
      // Not indexed
      // Not unique
      ;
}


table Protected
  (displayorder = 90)
  (category = "Data")
  (description = "A protected table") {

  (primary) Integer id
    (displaylevel = record) 
    (searchability = no);

  User spy
    (displaylevel = primary) 
    (searchability = primary)
    (displayorderpriority = 1)
    (description = "Spy")
    ;
  String mission
    (size = unlimited) 
    (displaylevel = primary) 
    (searchability = primary)
    (description = "Secret mission")
    (displayorderpriority = 0)
    (width = 20)
    ;

  Capability canRead
    (nullable)
    (displaylevel = record) 
    (description = "Capability required to read this row ")
    ;
  Capability canWrite
    (displaylevel = record) 
    (description = "Capability required to write this row ")
    ;
  Capability canDelete
    (displaylevel = record) 
    (description = "Capability required to delete this row ")
    ;
  Capability canSelect
    (displaylevel = record) 
    (nullable)
    (description = "Capability required to select this row ")
    ;
  Boolean deleted
    (displaylevel = record) 
    (description = "Whether this record is existant ")
    ;

}


table Dynamic
  (displayorder = 100)
  (category = "Data")
  (description = "A table for adding and deleting columns from") {

  (primary) Integer id
    (displaylevel = record) 
    (searchability = no);

  String name
    (size = unlimited) 
    (displaylevel = primary) 
    (searchability = primary)
    (description = "Name")
    (displayorderpriority = 0)
    (width = 20)
    ;


}
table User extends org.melati.poem.User 
    (displayorder = 2010)
    (description = "An emailed user")
    (category = "User") {
    
    String email 
      (size=unlimited)
      (nullable)
      (width=40)
      (description = "The user's email address")
      (displaylevel = summary)
      (displayorder = 50)
      (searchability = yes);

}

table Account 
    (displayorder = 2020)
    (description = "An account owned by a User")
    (category = "User") {
    
  (primary) Integer id
    (displaylevel = record) 
    (searchability = no);

    User user 
      (description = "The owner")
      (displaylevel = summary)
      (displayorder = 60)
      (searchability = yes);
    User StringKeyReference email
      (size=unlimited) 
      (width=40)
      (description = "The owner's email address")
      (displaylevel = summary)
      (displayorder = 50)
      (searchability = yes);
    String name
      (size=unlimited) 
      (nullable)
      (width=40)
      (description = "The user's email address")
      (displaylevel = summary)
      (displayorder = 150)
      (searchability = yes)
    ;
}
